import logo from '../logo.svg';

const Header = () => {
    return (
        <header>
            <h1>Component Hierarchy</h1>
            <img src={logo} alt="logo"/>
        </header>
    )
}

export default Header;